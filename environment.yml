name: paleoPCA-Cookbook
channels:
  - conda-forge
dependencies:
  - python<3.12  # hopefully temporary, to accommodate issues with Pythia binder
  - jupyter-book
  - jupyterlab
  - sphinx-pythia-theme
  - pip
  - pandas
  - xarray
  - s3fs
  - fsspec
  - numpy
  - cartopy
  - matplotlib
  - pip:
    - pyleoclim
    - eofs
    - nc_time_axis

